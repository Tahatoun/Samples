The finding is acknowledged. The reported class is generated by NSwag from the validated API contract and is not manually implemented application logic. NSwag is an approved framework within the AMSI environment and the generated model follows the contract definition used by the application.

The non-nullable properties may receive their CLR default values during model binding/deserialization when the corresponding fields are not provided. This behavior is known and accepted in this specific context because the default values are not trusted as valid business values by themselves.

Additional validation is performed after deserialization/model binding at the application/business layer before the object is processed. These validations ensure that required fields, business constraints, and invalid default values are checked before any sensitive operation is executed.

Therefore, the potential under-posting risk identified by Fortify is mitigated by downstream validation controls and by the fact that the model is generated from a controlled NSwag contract within an approved framework. The residual risk is considered acceptable.